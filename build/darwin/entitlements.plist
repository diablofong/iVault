<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    iVault v1.0 發布策略：直接發布 .app（zip 後上傳 GitHub Releases）
    不進行 code signing / notarization，使用者首次開啟右鍵→開啟即可。

    若未來申請 Apple Developer 帳號，在此加入：
      com.apple.security.cs.allow-unsigned-executable-memory = true  (WKWebView)
      com.apple.security.cs.disable-library-validation = true        (go-ios dylib)
      com.apple.security.app-sandbox = false                         (usbmuxd access)
-->
<plist version="1.0">
<dict/>
</plist>
